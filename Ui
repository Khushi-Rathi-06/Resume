        dashboard_cards(

            startup_score,

            opportunity_score,

            competition_score,

            investor

        )

        row1 = st.columns(2)

        with row1[0]:

            domain_card(

                domain,

                confidence

            )

        with row1[1]:

            target_customer_card(

                summary

            )

        st.divider()

        competitor_cards(

            categorized

        )

        st.divider()

        row2 = st.columns(2)

        with row2[0]:

            market_gap_card(

                gaps

            )

        with row2[1]:

            business_model_cards(

                business_models

            )

        st.divider()

        row3 = st.columns(2)

        with row3[0]:

            swot_grid(

                swot

            )

        with row3[1]:

            risk_card(

                risk

            )

        st.divider()

        verdict_card(

            verdict

        )
